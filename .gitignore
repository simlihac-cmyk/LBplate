# ========================
# 1. 가상환경 (Virtual Env)
# ========================
# 내 컴퓨터 설정이 남의 컴퓨터에 가면 안 됨
venv/
env/
.env

# ========================
# 2. Python & Django 기본
# ========================
# 파이썬이 실행할 때 만드는 임시 파일들
__pycache__/
*.py[cod]
*$py.class

# ========================
# 3. 데이터베이스 (중요!)
# ========================
# ★ 절대 올리지 마세요!
# 이유: 개발하다가 실수로 'git pull' 하면, 
# 서버에 있던 실제 유저 데이터가 개발용 데이터로 덮어씌워져서 다 날아갑니다.
db.sqlite3
db.sqlite3-journal

# ========================
# 4. 정적 파일 모음 (collectstatic)
# ========================
# 서버에서 명령어로 생성하는 폴더이므로 올릴 필요 없음
staticfiles/

# ========================
# 5. 미디어 파일 (유저 업로드)
# ========================
# 사용자가 올린 이미지 등은 코드에 포함되면 안 됨
media/

# ========================
# 6. AI 모델 파일 (대용량)
# ========================
# 100MB 넘어가면 깃허브 업로드 차단됨 (4GB짜리 모델 필수 제외)
models/
*.vec
*.bin
*.model

# ========================
# 7. OS & 에디터 설정 파일
# ========================
# 맥(Mac)이 만드는 쓰레기 파일
.DS_Store

# VS Code 설정 폴더
.vscode/
.idea/